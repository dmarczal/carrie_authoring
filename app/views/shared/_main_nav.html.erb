<div id="navigation">
  <ul class="nav nav-tabs">
    <li><%= menu_link_to "Home", root_path %></li>
    <% if user_signed_in? && (current_user.professor? || current_user.admin?) %>
      <li><%= menu_link_to "OAs", learning_objects_path %></li>
    <% end %>
    <li><%= menu_link_to "Fractals", fractals_path %></li>
    <% if user_signed_in? %>
      <li><%= menu_link_to "Minhas turmas", learning_groups_path %></li>
      <li><%= menu_link_to "Meu perfil", edit_user_registration_path %></li>
    <% end %>
    <li><a href="#">Ajuda</a></li>
    <li><a href="#">Sobre</a></li>
    <% if user_signed_in? %>
      <span class="user_logged">
        <%= current_user.email %>
        <%= link_to 'Sair', destroy_user_session_path, :method => "delete", :class => "btn medium" %>
      </span>
    <% else %>
      <span class="user_logged">
       <%= link_to 'registrar-se', new_user_registration_path,  :class => "btn" %>
       <%= link_to 'fazer login', new_user_session_path,  :class => "btn" %>
      </span>
    <% end %>
  </ul>
</div>

